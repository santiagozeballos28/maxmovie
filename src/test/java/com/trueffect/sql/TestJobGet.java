
package com.trueffect.sql;

import com.trueffect.response.ErrorResponse;
import com.trueffect.validation.ProcessObject;
import com.trueffect.validation.RenterUser;
import com.trueffect.model.Job;
import com.trueffect.model.Person;
import org.junit.Assert;
import org.junit.Test;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

/**
 *
 * @author santiago.mamani
 */
public class TestJobGet {
    
    /*
     @Test
    public void testJogGet() throws Exception {

        Job job = new Job(1,"Administrator");
        //RenterUserComply complyRenterUser = mock(RenterUser.class);
       // when(complyRenterUser.complyCondition(1,renterUser)).thenReturn(true);
        String ex_msg = "";
        Job jog_get = null;
        try {
            jog_get =  SqlJob.getJobOf(14);
        } catch (ErrorResponse ex) {
            ex_msg = ex.getMessage();
        }
        Assert.assertEquals(jog_get.equals(job),true);
    }
*/
}
